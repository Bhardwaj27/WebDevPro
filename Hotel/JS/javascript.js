

// Automatically changing slide javascript
var myIndex = 0;
carousel();
// function to change image after 3 second
function carousel() {
  var i;
  // getting slides i.e. images by classname 
  var x = document.getElementsByClassName("mySlides");
  for (i = 0; i < x.length; i++) {
    // making display of all images none
    x[i].style.display = "none";  
  }
  myIndex++;
  if (myIndex > x.length) {myIndex = 1}    
  x[myIndex-1].style.display = "block";  
  setTimeout(carousel, 3000); // Change image every 3 seconds
}

// changing discount color to red and yellow continously
var color = false;
setInterval(() => {
  if (color) {
    document.getElementById('discount').style.color = `red`;
  }
  else
    document.getElementById('discount').style.color = `yellow`;
color = !color;
}, 1000);

// onclick function after form submit
// setting local storage
function formBtn() {
  var nam = document.getElementById('name').value;
  // console.log(nam);
  localStorage.setItem(`name`, nam);
  var emai = document.getElementById('email').value;
  // console.log(emai);
  localStorage.setItem(`email`, emai);
  var rea = document.getElementById('reason').value;
  // console.log(rea);
  localStorage.setItem(`reason`, rea);
  alert(`Congrats!! Details Sent Successfully..`);
}






